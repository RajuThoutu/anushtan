# ─── PostgreSQL Database (Hybrid Zero Trust Architecture) ────────────────────
# LOCAL DEV (.env.local) → Cloudflare Tunnel bridge (127.0.0.1:5433 → VPS n8n_dev)
# Tunnel must be active: `cloudflared tunnel run`
DATABASE_URL="postgres://n8n_admin:<password>@127.0.0.1:5433/n8n_dev?sslmode=disable"
#
# VERCEL PRODUCTION (.env / Vercel env var) → Direct IP, Vercel CIDR whitelisted
# DATABASE_URL="postgres://n8n_admin:<password>@72.62.230.141:5432/n8n_prod?sslmode=disable"
#
# VERCEL PREVIEW/DEV (Vercel env var) → Direct IP to n8n_dev
# DATABASE_URL="postgres://n8n_admin:<password>@72.62.230.141:5432/n8n_dev?sslmode=disable"

# ─── Google Sheets (Downstream sync mirror) ──────────────────────────────────
# Master feature flag — set "true" only when n8n pipeline is ready and validated
ENABLE_GOOGLE_SHEETS_SYNC="false"
# Legacy flag (used by packages/database/client.ts direct Sheets sync)
SHEETS_SYNC_ENABLED="false"
GOOGLE_SHEET_ID="your_sheet_id_here"
GOOGLE_SHEETS_CLIENT_EMAIL="your_service_account_email@project.iam.gserviceaccount.com"
# Private key — full PEM string including headers
GOOGLE_SHEETS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ─── n8n Webhook (Sheets sync trigger) ───────────────────────────────────────
# DEV
N8N_WEBHOOK_URL="http://localhost:5678/webhook/school-inquiry"
# PROD
# N8N_WEBHOOK_URL="https://api.anushtansiddipet.in/webhook/school-inquiry"

# ─── NextAuth (Admin App) ─────────────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your_nextauth_secret_min_32_chars"

# ─── Sanity (Web App) ────────────────────────────────────────────────────────
NEXT_PUBLIC_SANITY_PROJECT_ID="your_sanity_project_id"
NEXT_PUBLIC_SANITY_DATASET="production"
NEXT_PUBLIC_SANITY_API_VERSION="2024-01-01"

# ─── Twilio / WhatsApp ───────────────────────────────────────────────────────
TWILIO_ACCOUNT_SID="AC..."
TWILIO_AUTH_TOKEN="your_auth_token"
TWILIO_FROM_NUMBER="+1234567890"
